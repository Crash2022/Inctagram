import type { Ni18nOptions } from 'ni18n'

// export const ni18nConfig: { supportedLngs: string[]; ns: string[] } = {
export const ni18nConfig: Ni18nOptions = {
    supportedLngs: ['en', 'ru'],
    ns: ['home', 'header', 'sidebar']
}
